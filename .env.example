# Copy this file to `.env` and fill in values. `.env` is gitignored.
#
# NOTE: this only affects LOCAL development (Expo Go / dev builds). EAS cloud
# builds do NOT receive this file — it is gitignored, so it is never uploaded.
# The API URL used by real builds lives in `eas.json` under each build profile.

# Base URL of the Prem Expense Tracker API (must include the /api/v1 suffix).
# Local FastAPI (physical device via Expo Go needs your machine's LAN IP, not localhost):
#   EXPO_PUBLIC_API_BASE_URL=http://192.168.1.5:8000/api/v1
# Production (Render):
#   EXPO_PUBLIC_API_BASE_URL=https://prem-expense-tracker.onrender.com/api/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
